{% extends "::base.html.twig" %}

{% block stylesheets %}
    {{ parent() }}
    {% stylesheets
        "bundles/imperivsite/css/style.css"
    filter="yui_css, cssrewrite"%}
        <link href="{{ asset_url }}" type="text/css" rel="stylesheet" />
    {% endstylesheets %}
    <style>
        .embed-container {
            position: relative;
            padding-bottom: 56.25%;
            height: 0;
            overflow: hidden;
            max-width: 100%;
        }
        .embed-container iframe, .embed-container object, .embed-container embed {
            position: absolute;
            top: 0; left: 0;
            width: 100%;
            height: 100%;
        }
        img { width: 100%; }
        body { background-image: url('http://fc07.deviantart.net/fs70/f/2013/174/3/e/recycled_texture_background_by_sandeep_m-d6aeau9.jpg'); }
    </style>
    <script src="{{ asset("bundles/imperivsite/js/modernizr.js") }}" type="text/javascript"></script>
{% endblock stylesheets %}

{% block title %}
    Video editing
{% endblock title %}

{% block content %}
    <div class="container-fluid">
        <div class="col-md-9">
            <table class="table">
                <tr>
                    {% for video in videos %}
                        <td class="effect-{{ random(4) }} video embed-container">
                            <figure>
                                {#<a href=""  data-lightbox="{{ video.video.name }}">#}
                                    {% thumbnail video.video, 'big' %}
                                {#</a>#}
                                <figcaption>
                                    <h3>{{ video.video.name }}</h3>
                                    <a href="#" class="close-caption hidden">x</a>
                                </figcaption>
                            </figure>
                        </td>
                        {% if loop.index%3 == 0 %}
                            </tr><tr>
                        {% endif %}
                    {% endfor %}
                </tr>
            </table>
        </div>
        <div class="col-md-3">
            {#<button id="button">Hello world</button>#}
        </div>
    </div>
{% endblock content %}

{% block javascripts %}
    {% javascripts
        "bundles/imperivsite/js/touch.js"
    filter="yui_js" %}
        <script src="{{ asset_url }}" type="text/javascript"></script>
    {% endjavascripts %}
{% endblock javascripts %}