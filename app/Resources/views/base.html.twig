<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}3D Design Studio{% endblock title %}</title>

        {% block stylesheets %}
            {% stylesheets
                "@ImperivSiteBundle/Resources/public/css/bootstrap.min.css"
                "@ImperivSiteBundle/Resources/public/css/main.css"
                filter="yui_css"
            %}
                <link href="{{ asset_url }}" type="text/css" rel="stylesheet" />
            {% endstylesheets %}
        {% endblock stylesheets %}

        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>

    <body>
        <header>
            <ul class="navigator">
                <li>
                    <a href="{{ path('gallery_index', {'name':'Jack White'}) }}" {{ (app.request.get('_route') == 'gallery_index') ? "class=\"selected\"" }}>
                        Home
                    </a>
                </li>
                <li>
                    <a href="#">3D Design</a>
                </li>
                <li>
                    <a href="#">Graphic Art</a>
                </li>
                <li>
                    <a href="#">Video Editing</a>
                </li>
                <li>
                    <a href="#">Models</a>
                </li>
                <li>
                    <a href="#" id="explore" >Explore more</a>
                </li>
            </ul>
        </header>

        {% block content %}{% endblock content %}
            
        {% block javascripts %}
            {% javascripts
                "@ImperivSiteBundle/Resources/public/js/jquery_2.1.3.min.js"
                filter="yui_js"
            %}
                <script src="{{ asset_url }}" type="text/javascript"></script>
            {% endjavascripts %}
        {% endblock javascripts %}
    </body>
</html>